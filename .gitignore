# Python cache
__pycache__/
*.pyc
*.pyo

# Word temp/lock files
~$*

# Local planning docs — not part of the deployable system
Local files for Convention/

# Excel files — may contain sensitive delegate data
*.xlsx
*.xls

# Credentials snapshots — contain delegate PII
surrendered_delegates.json

# Convention admin — local only, not deployed
convention_admin.py
convention_admin_backup.py
convention_admin_fixed.py
convention_admin_tmp.py

# Setup data — local convention data, not deployed
setup_data/

# Temp JSON files (generated by setup script or sandbox)
tmp*.json

# OS files
.DS_Store
Thumbs.db
Pre-Convention_Action_List_May1.docx
